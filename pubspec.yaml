name: dep_gen
description: Generates methods for creating instances of classes with automatic dependency substitution.
version: 1.0.12
homepage: https://github.com/dadrum/dep_gen.git

environment:
  sdk: '>=2.17.1 <3.0.0'

dependencies:
  analyzer: ^5.2.0
  args: ^2.3.2
  path: ^1.8.2
  yaml: ^3.1.1

dev_dependencies:
  lints: ^2.0.1

